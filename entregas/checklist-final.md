# Checklist final

- [x] Pastas exclusivas `html`, `css`, `imagens` e `js`.
- [x] SPA por hash com interceptação e renderização no DOM.
- [x] Templates dinâmicos com Template Literals, `map()` e `innerHTML`.
- [x] Eventos `click`, `submit`, `input`, `change`, `blur`, `keydown` e `hashchange`.
- [x] Validação de formulário com estados de erro/sucesso.
- [x] Persistência com `localStorage`, `JSON.stringify()` e `JSON.parse()`.
- [x] Uso de Vanilla JavaScript documentado; nenhuma biblioteca externa necessária.
- [x] ES6 Modules com `import`/`export` e relatório de bugs/correções.
