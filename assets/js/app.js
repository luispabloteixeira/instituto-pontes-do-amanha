const menuButton=document.querySelector('.menu-toggle');const menu=document.querySelector('#menu');if(menuButton&&menu){menuButton.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))})}
const onlyDigits=value=>value.replace(/\D/g,'');
const masks={cpf:value=>onlyDigits(value).slice(0,11).replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d{1,2})$/,'$1-$2'),telefone:value=>{const v=onlyDigits(value).slice(0,11);return v.length>10?v.replace(/(\d{2})(\d{5})(\d{4})/,'($1) $2-$3'):v.replace(/(\d{2})(\d{4})(\d{0,4})/,'($1) $2-$3')},cep:value=>onlyDigits(value).slice(0,8).replace(/(\d{5})(\d)/,'$1-$2')};
Object.entries(masks).forEach(([name,mask])=>{const field=document.querySelector(`[name="${name}"]`);if(field)field.addEventListener('input',()=>{field.value=mask(field.value)})});
const form=document.querySelector('#cadastro');if(form){form.addEventListener('submit',event=>{event.preventDefault();const status=document.querySelector('#form-status');if(!form.checkValidity()){status.textContent='Revise os campos destacados antes de continuar.';status.className='status error';form.reportValidity();return}status.textContent='Cadastro validado com sucesso. Demonstração concluída sem envio de dados.';status.className='status success'})}

const birthDate=document.querySelector('[name="nascimento"]');if(birthDate){const today=new Date();birthDate.max=[today.getFullYear(),String(today.getMonth()+1).padStart(2,"0"),String(today.getDate()).padStart(2,"0")].join("-")}
